package utils

func AddCsv() {
	// clothBrand := db.MogShoeSession()
	// csvfile, err := os.Open("./config/brand1.csv")
	// if err != nil {
	// 	log.Fatalln("Couldn't open the csv file", err)
	// }

	// // Parse the file
	// r := csv.NewReader(csvfile)
	// //r := csv.NewReader(bufio.NewReader(csvfile))

	// // Iterate through the records
	// for {
	// 	// Read each record from csv
	// 	record, err := r.Read()
	// 	if err == io.EOF {
	// 		break
	// 	}
	// 	if err != nil {
	// 		log.Fatal(err)
	// 	}
	// 	fmt.Printf("1==>: %s 2===> %s 3===>%s\n", record[0], record[1], record[2])

	// 	clothBrand.Insert(&model.ShoeSize{ShoeSize: record[0] + "," + record[1] + "," + record[2]})

	// }
}
